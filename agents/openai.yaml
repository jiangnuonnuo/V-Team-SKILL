interface:
  display_name: "V-Team"
  short_description: "Multi-agent project skill: identity, whitelist, plan approval, risk tests, check-scope"
  default_prompt: "Use $v-team to set up project-local multi-agent collaboration: confirm agent identity and business scope, write one approval-gated PLAN.md, enforce module boundaries with check-plan/check-scope, and keep Plan/ handoffs local (never commit them)."
