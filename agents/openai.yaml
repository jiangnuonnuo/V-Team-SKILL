interface:
  display_name: "V-Team"
  short_description: "按功能选择最小角色链路，确认方案后连续交付"
  default_prompt: "使用 $v-team 判断任务路线；只对标准或重大功能先给紧凑方案并等待一次确认，之后按需切换角色、通过契约对接并连续完成。"

policy:
  allow_implicit_invocation: false
