interface:
  display_name: "V-Team"
  short_description: "Multi-agent project skill: identity, whitelist, plan approval, handoff list/create, check-scope"
  default_prompt: "Use $v-team to set up project-local multi-agent collaboration: confirm agent identity and business scope, write one approval-gated PLAN.md, use handoff list to read exact handoff docs (never scan active/), create handoffs only when another agent must integrate, enforce check-plan/check-scope, and keep Plan/ local (never commit them)."
