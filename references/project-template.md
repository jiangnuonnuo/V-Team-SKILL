# 项目当前状态

## 项目目标

- 待用户补充。

## 当前阶段

- 状态：初始化完成。
- 当前重点：注册 Agent，并为每个身份确认职责和白名单。

## 模块边界

| 模块 | 主要职责 | 默认负责 Agent | 备注 |
|---|---|---|---|

## 全局约束

- 本文件只保存当前有效事实，新需求优先覆盖或修订原段落。
- 已失效的临时对接材料在关闭 handoff 后由 `cleanup` 删除，不保留在活动上下文中。
- 不为每次对话或普通需求创建重复项目状态文件。
