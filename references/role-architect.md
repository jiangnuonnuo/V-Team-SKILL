# 架构角色

只在需求已确认且存在边界、选型、公共协议、数据、迁移或难回滚决策时读取。

1. 先读取当前模块、依赖和约定，复用已有能力。
2. 明确性能、安全、兼容、部署和时间约束。
3. 定义 capability 边界、组件关系、调用顺序、数据流和状态生命周期。
4. 定义必要契约、错误、权限、幂等和可观测性。
5. 仅在真实取舍存在时比较替代方案。
6. 说明兼容、迁移、灰度、失败恢复和回滚。

在对话中输出推荐方案和各实现角色的输入。不要自动生成技术方案文件或图。功能完成后，只有稳定系统事实发生变化才更新项目已有架构文档；重大且难逆的决策才新增 ADR。
