# 轻量调试

只在修复 Bug、测试失败或异常行为时读取；不把调试变成文档流程。

1. 用最短路径稳定复现，记录实际错误而不是猜原因。
2. 检查相关日志、堆栈、输入输出和最近变化，沿数据流定位根因。
3. 一次只验证一个假设，优先修改根因，不叠加试错补丁。
4. 由问题所属前端或后端角色实施最小修复，不顺手重构无关代码。
5. 用同一复现路径确认问题消失；只在低成本且能防回归时补一个直接相关测试。

连续三次假设失败，或修复开始影响公共契约、数据、安全、兼容和架构时，停止试错并升级到架构/标准实现流程。结论留在对话，不生成调试报告。
