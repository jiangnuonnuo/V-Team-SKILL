---
name: v-team
description: 仅在用户明确调用 `$v-team` 时，为功能开发选择最小的需求、架构、后端、前端和 QA 链路；重要功能先确认方案，再开发并用契约完成跨角色对接。未明确调用时不要使用。
---

# V-Team

把 V-Team 当作个人功能交付流程，不当作多 Agent 编排器。角色按职能和功能临时切换，同一 Agent 可以连续承担多个角色。

## 先选一条路线

- **问答/分析**：用户未要求修改。直接回答；不读任何 reference，不建 ID，不写状态。
- **快速改动**：结果明确、局部、易回滚，且不改变公共 API、Schema、数据、权限、安全或架构。直接实现并做最小充分验证；不读 reference，不写状态，不等待方案确认。
- **标准功能**：新增用户能力、行为或 UI 流程，或者存在产品/技术取舍。先给决策摘要，用户确认一次后实现。
- **重大改造**：跨核心模块、破坏性协议、数据迁移、安全边界、核心架构或难回滚变化。走标准功能流程，并按需保留恢复状态或里程碑。

问答和快速改动选定后立即停止 V-Team 路由，不继续执行下面的角色、契约和留档步骤。判断不清或执行中出现公共契约、数据、安全、兼容、架构影响时，升级路线。

## 标准/重大功能：确认一次再连续执行

先只读检查当前项目，然后在对话中给出与复杂度相称的决策摘要：

1. 要解决的问题、价值，以及现有能力能否复用；
2. 目标、非目标和可验证验收条件；
3. 推荐方案、关键风险和真实存在的取舍；
4. 仅在相关时说明架构、契约、迁移和回滚；
5. 所需的最小角色链路与顺序。

没有真实选择时不要凑替代方案，不生成 PRD、设计稿或 PLAN 文件。请求用户确认整份摘要一次；用户已经批准完整方案时不要重复确认。

批准后连续执行到完成。只有出现新的产品决策、方案发生实质变化或无法自行解决的阻塞时才再次停下，不要按角色或小步骤反复请示。

## 角色按阶段即时读取

不要预读所有角色。进入某一阶段前只读该角色参考，阶段结束后再决定下一角色：

| 何时需要 | 读取 |
|---|---|
| 价值、范围或验收仍不清楚 | [role-requirement.md](references/role-requirement.md) |
| 存在边界、选型、公共协议、数据或迁移决策 | [role-architect.md](references/role-architect.md) |
| 开始后端实现 | [role-backend.md](references/role-backend.md) |
| 开始 UI/前端实现和联调 | [role-frontend.md](references/role-frontend.md) |
| 风险需要独立验收或跨角色闭环 | [role-qa.md](references/role-qa.md) |

纯后端不读前端参考，已有稳定接口的页面通常不读架构参考，低风险功能不机械增加 QA 角色。

只有跨角色契约或跨会话状态确实存在时，才创建稳定 `capability_id` 和 `<职能>-<功能范围>` 角色 ID；普通单角色任务使用角色职能即可。

## 只在跨角色时启用契约

前后端或其他提供者/消费者需要对接时，读取 [contract-policy.md](references/contract-policy.md)：

- 契约正文留在项目已有 OpenAPI、Schema、protobuf、共享类型或契约测试中；
- 项目已有 API Catalog/Schema Registry 时直接复用；否则只用 `.vteam/state.json` 保存轻量索引；
- 消费者按 `capability + 完整角色 ID` 发现；零个就阻塞，多个就明确选择，禁止猜测；
- `draft` 只供 mock，`ready`/`verified` 才能真实联调。

不涉及跨角色消费时，不运行契约命令，也不为“可能以后使用”创建索引。

## 状态和里程碑按需读取

普通功能不创建 V-Team 文件。只有契约索引、明确跨会话恢复或重大里程碑需要持久化时，才读取 [milestone-policy.md](references/milestone-policy.md) 并使用唯一 `.vteam/state.json`。

若状态已因契约或恢复而存在，模块闭环后用 `module complete` 覆盖当前事实并清除 active；否则普通完成不专门建状态。日常测试输出、分析、方案和总结只留在对话中。

## 实现与完成

按当前角色交付可验收的完整结果，不按文件拆角色。按风险选择单元、契约、集成、浏览器或端到端测试；覆盖与功能有关的加载、空、错误、权限、网络、重复提交、兼容和回滚边界。

完成必须满足：功能闭环可用、验收条件通过、必要测试真实通过、跨角色契约可定位且稳定、临时 active 已清除。只有稳定架构边界确实变化时才更新项目已有架构事实；普通完成不新建总结或 ADR。

V-Team 不要求提交、分支、push、merge 或 PR；只在用户请求或项目规则要求时执行。
