---
name: v-team
description: 仅在用户明确调用 `$v-team` 时，为单个产品 capability 选择最小的需求、方案、架构、前端、后端、联调、评审及按需运维链路；标准和重大功能始终评估 frontend/backend 双 Lane，跨角色用契约，重复操作复用已验证 Playbook。未明确调用时不要使用。
---

# V-Team

把 V-Team 当作个人功能交付流程，不当作固定瀑布或多 Agent 编排器。同一 Agent 可以按阶段切换角色；标准和重大产品功能也可以将前端、后端分给独立执行者，通过契约交接。

## 先判断模式、风险和功能链路

### 1. 操作模式

- **回答**：解释、理解或状态查询；不改代码，不读 reference，不建 ID，不写状态。
- **评估/设计**：只做需求、方案、UI 或架构设计；只读必要 reference，在对话中交付结论后停止。
- **实现**：按风险选择最小功能链路，连续完成直到验收。
- **修复**：读取 [debugging-policy.md](references/debugging-policy.md) 与问题所属 Lane；先复现、定位根因，再做最小修复。公共契约、数据、安全或架构受影响时升级为实现。
- **恢复**：读取当前 capability、Lane、active 和相关契约，直接进入记录的功能环节；方案未实质变化时不重复分析或确认。

### 2. 风险等级

- **快速**：局部、明确、易回滚，且不改变公共 API、Schema、数据、权限、安全或架构；直接完成，只验证直接结果。
- **标准**：新增用户能力、行为或 UI 流程，或存在真实取舍；先形成紧凑摘要，确认一次后实现。
- **重大**：跨核心模块、破坏性协议、数据迁移、安全边界、核心架构、生产发布或难回滚变化；按真实影响补齐设计、恢复和验证。

影响不清时升级风险；不要因文件数量或流程名称机械加重任务。

### 3. Capability 与双 Lane

标准和重大**产品功能**以一个 capability 组织。进入开发前必须显式评估 `frontend` 与 `backend` 两条 Lane：`required`、`no-change` 或 `blocked`。无改动的一侧仍保留 `no-change` 和依据，不能静默省略；快速任务只在对话中完成同样判断，不为此创建状态。

功能链路是自适应的：发现 → 需求判断 → 方案/架构实施设计 → 前端与后端实现 → 联调 → 评审 → 按需运维。没有真实问题、取舍、跨角色消费或发布时，合并或跳过对应环节。

## 按需读取功能与角色规则

进入阶段前只读一个必要参考，结束后再决定下一项；不要预读所有角色。

| 条件 | 读取 |
|---|---|
| 标准/重大功能、复用不明或需要建立双 Lane | [capability-policy.md](references/capability-policy.md) |
| 价值、范围或验收不清 | [role-requirement.md](references/role-requirement.md) |
| 存在边界、选型、协议、数据、迁移或实施顺序决策 | [role-architect.md](references/role-architect.md) |
| 开始后端 Lane | [role-backend.md](references/role-backend.md) |
| 开始前端 Lane 或真实联调 | [role-frontend.md](references/role-frontend.md) |
| 风险需要功能、架构或代码质量评审 | [role-qa.md](references/role-qa.md) |
| 用户要求部署或验收范围包含发布 | [role-operations.md](references/role-operations.md) |
| 已验证操作可能复用或需要沉淀 | [playbook-policy.md](references/playbook-policy.md) |

能力发现先搜索项目代码、现有模块、可用 Skills 和项目约定；信息易变、技术陌生、风险较高或用户要求时再查询官方资料。证据足够支持当前决策时停止搜索。

## 标准/重大实现只确认一次

先检查项目，再给出与复杂度相称的摘要：

1. 问题、价值和现有能力复用；
2. 目标、非目标和验收条件；
3. 推荐方案、关键风险和真实取舍；
4. 相关时说明架构、契约、迁移、回滚和部署；
5. capability 的 frontend/backend Lane 状态、实现顺序与联调方式。

没有真实选择时不要凑替代方案，不生成 PRD、设计稿或 PLAN 文件。请求用户确认整份摘要一次；批准后连续执行，只有新的产品决策、实质方案变化或无法解决的阻塞才停下。

## 前后端联调与恢复

提供者在项目权威来源定义公共契约：后端可发布 `draft`，实现并取得直接证据后发布 `ready`；前端只用 `draft` 做明确标记的 mock，`ready/verified` 后才真实联调。消费者按完整角色 ID 和 capability 发现：一个匹配直接使用，多个明确选择，零个或 blocked 则报告阻塞，禁止猜测接口。

普通任务不创建 V-Team 文件。跨角色契约、跨会话恢复、已验证 Playbook 或重大里程碑需要持久化时，使用唯一 `.vteam/state.json`。状态只保存当前事实和项目引用，不保存方案正文、接口字段、日志或凭据。

## 运维、评审与完成

部署只在用户明确要求或任务范围包含发布时进入。先复用已验证 Playbook 或项目 Runbook，确认环境、制品、健康检查和回滚来源；V-Team 不自动执行任意部署命令，也不保存密钥。

- 快速改动：验证直接结果。
- 标准功能：验证关键主流程、架构符合性和直接受影响质量检查。
- 重大改造：只扩大到实际兼容、数据、安全、性能、发布和回滚范围。

评审失败时回到最早出错的环节：需求、方案、架构实施设计、对应 Lane、契约或运维。完成要求：功能闭环可用、验收满足、两条 Lane 均为 `completed/no-change`、相关契约稳定、临时 active 已清除。不要默认全量测试、重复测试、独立一致性审查或测试报告。

普通完成不新建总结或 ADR；只有稳定架构事实确实变化时才维护项目已有文档。

V-Team 不要求提交、分支、push、merge、PR 或发布；只在用户请求或项目规则要求时执行。
