---
name: v-team
description: 仅在用户明确调用 `$v-team` 时，为功能评估、设计、实现、修复或恢复选择最小的需求、架构、后端、前端和 QA 链路；重要功能先确认方案，跨角色时用契约对接。未明确调用时不要使用。
---

# V-Team

把 V-Team 当作个人功能交付流程，不当作多 Agent 编排器。同一 Agent 可以按阶段切换角色。

## 先判断三个维度

### 1. 操作模式

- **回答**：解释、理解或状态查询；不改代码，不读 reference，不建 ID，不写状态。
- **评估/设计**：只做需求、UI、架构或技术方案；只读对应角色 reference，在对话中交付结果后停止，不进入实现或留档。
- **实现**：按风险等级决定是否先确认方案，再由所需角色连续完成。
- **修复**：读取 [debugging-policy.md](references/debugging-policy.md) 和所属实现角色；先复现、定位根因，再做最小修复。若发现公共契约、数据、安全或架构变化，升级为实现模式。
- **恢复**：已有 capability 时读取当前 context、active 和相关契约，直接进入记录的下一角色；方案未实质变化时不重复分析或确认。

### 2. 风险等级

- **快速**：结果明确、局部、易回滚，且不改变公共 API、Schema、数据、权限、安全或架构；直接完成，不读角色 reference、不写状态、不等待方案确认。
- **标准**：新增用户能力、行为、UI 流程或真实产品/技术取舍；先给紧凑决策摘要，确认一次后实现。
- **重大**：跨核心模块、破坏性协议、数据迁移、安全边界、核心架构或难回滚变化；走标准流程，并按需保留恢复状态或里程碑。

判断不清或影响扩大时升级风险，不因任务看起来复杂就机械增加流程。

### 3. 当前角色

不要预读所有角色。进入阶段前只读一个必要参考，结束后再决定下一角色：

| 条件 | 读取 |
|---|---|
| 价值、范围或验收不清 | [role-requirement.md](references/role-requirement.md) |
| 存在边界、选型、协议、数据或迁移决策 | [role-architect.md](references/role-architect.md) |
| 开始后端实现 | [role-backend.md](references/role-backend.md) |
| 开始 UI/前端实现和联调 | [role-frontend.md](references/role-frontend.md) |
| 高风险或跨角色闭环需要独立验收 | [role-qa.md](references/role-qa.md) |

纯后端不读前端参考，已有稳定接口的页面通常不读架构参考，低风险功能不机械增加 QA。只有跨角色契约或跨会话状态存在时才创建稳定 `capability_id` 和 `<职能>-<功能范围>` 角色 ID。

## 标准/重大实现只确认一次

先只读检查项目，再在对话中给出与复杂度相称的摘要：

1. 问题、价值和现有能力复用；
2. 目标、非目标和验收条件；
3. 推荐方案、关键风险和真实取舍；
4. 仅在相关时说明架构、契约、迁移和回滚；
5. 最小角色链路与顺序。

没有真实选择时不要凑替代方案，不生成 PRD、设计稿或 PLAN 文件。请求用户确认整份摘要一次；用户已经批准时不要重复确认。批准后连续执行，只有新的产品决策、实质方案变化或无法解决的阻塞才停下。

## 跨角色契约

仅在提供者和消费者需要对接时读取 [contract-policy.md](references/contract-policy.md)。正文留在项目 OpenAPI、Schema、protobuf、共享类型或契约测试中；已有 Catalog/Registry 时复用，否则 `.vteam/state.json` 只存索引。

后端以真实 `source_ref` 发布 draft；实现并取得一次直接证据后发布 ready。前端冷启动时按完整角色 ID 读取 context：不知道 capability 就先查看待消费契约，一个匹配直接使用，多个明确选择，没有则阻塞。draft 只供 mock，ready/verified 才能真实联调。

不涉及跨角色消费时，不运行契约命令，也不为将来可能使用创建索引。

## 状态与恢复

普通功能不创建 V-Team 文件。只有契约索引、明确跨会话恢复或重大里程碑需要持久化时，才读取 [milestone-policy.md](references/milestone-policy.md) 并使用唯一 `.vteam/state.json`。

状态已存在时，模块闭环后用 `module complete` 覆盖当前事实并清除 active；否则普通完成不专门建状态。恢复任务不重走已完成角色，不重新加载无关历史。

## 最小验证与完成

- 快速改动：只验证直接结果。
- 修复：用同一复现路径确认前后变化；只在低成本且能防回归时增加一个相关测试。
- 标准功能：验证关键主流程或直接受影响测试。
- 重大改造：只扩大到实际受影响范围。

不要默认全量测试、重复测试、独立一致性审查、测试报告或按角色重复验收。报告实际执行过的结果，不能用“应该通过”代替证据。

完成要求：功能闭环可用、验收条件满足、跨角色契约可定位且稳定、临时 active 已清除。只有稳定架构事实确实变化时才更新项目已有架构文档；普通完成不新建总结或 ADR。

V-Team 不要求提交、分支、push、merge 或 PR；只在用户请求或项目规则要求时执行。
